// src/bot/init/index.ts
/**
 * Barrel file for bot initialization
 * Re-exports the initialization function from init.ts
 */

// Import and re-export the initialization function
export { initializeBot, default } from './init';